The main program is within the Wordle Solver Folder.
There is a folder inside of that that contains each dataset after each guess.

The files outside all contain the data used to determine the best first guess. 
The program uses a CLI interface on the terminal to work. A window GUI will be made at a later time.
